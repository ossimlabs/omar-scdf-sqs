spring:
  application:
    name: omar-download
  cloud:
    config:
      enabled: false
      uri: http://localhost:8888/omar-config-server
    discovery:
      enabled: false
---
spring:
  profiles: production
  cloud:
    config:
      uri: http://scdf-config-server:8888/scdf-config-server
