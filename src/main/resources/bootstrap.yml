spring:
  application:
    name: omar-scdf-sqs
  cloud:
    config:
      enabled: false
      uri: http://localhost:8888/omar-config-server
    discovery:
      enabled: false
---
spring:
  profiles: production
  cloud:
    config:
      uri: http://scdf-config-server:8888/omar-config-server
