spring:
  application:
    name: omar-scdf-sqs
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888/omar-config-server
    discovery:
      enabled: false
---
spring:
  application:
    name: omar-scdf-sqs
  profiles:
      active: production
  cloud:
    config:
      enabled: true
      uri: http://scdf-config-server:8888/omar-config-server
    discovery:
      enabled: false
